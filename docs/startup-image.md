# Startup image

Asset: `xyz.brbc.youtube.iinaplugin/assets/YouTube.png`.

Created with the built-in image-generation tool, replacing the original red gradient. The generated PNG is copied unchanged into the plugin bundle.

Prompt:

> Create a replacement PNG startup splash screen for a YouTube browsing plugin in IINA on macOS. Landscape 16:9, ideally 1920x1080. Minimal polished native media-player welcome screen, almost-black charcoal background with extremely subtle dark burgundy glow near the center. Center a small tasteful red rounded rectangle play symbol, below it large crisp off-white text exactly 'YouTube for IINA', then smaller muted gray text exactly 'Find something worth watching.' Generous empty space and quiet refined typography, flat graphic design, no browser chrome, no mockup device, no extra text, no bright full-screen red, no photographic objects. This is the actual startup PNG shown behind a sidebar.
